doctype
html(prefix='og: http://ogp.me/ns#', lang='de')
  head
    include metatags
    include styles
  block body
    include header
    main.margin-top-sm-51.bg-texture-main(role='main')
      .container
        if user
          h1= user.username
          p you are authenticated
        else
          p Go and log urself in
    include footer
